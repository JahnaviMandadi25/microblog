pipeline {
    agent any

    stages {
        stage('SCM') {
            steps {
                // Checking out the source code from SCM
                checkout scm
            }
        }

        stage('Build') {
            steps {
                sh 'echo Building the application...'
                // Add your build commands here
            }
        }

        stage('SonarQube Analysis') {
            environment {
                SONAR_TOKEN = 'sqp_628b9170394ec9f586f8934cd11ba1da99bd2c47'
            }
            steps {
                script {
                    // Trigger SonarQube analysis
                    def scannerHome = tool 'SonarScanner'  // Use the correct SonarQube Scanner tool name
                    withSonarQubeEnv('SonarQube') {
                        sh "${scannerHome}/bin/sonar-scanner -Dsonar.login=${env.SONAR_TOKEN}"
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo Deploying the application...'
                // Add your deployment commands here, e.g., Docker, file transfer, etc.
            }
        }

        stage('Integration Tests') {
            steps {
                sh 'echo Running integration tests...'
                // Add your Selenium or other integration test commands here
            }
        }
    }

    post {
        always {
            echo 'Cleaning up workspace...'
            cleanWs()
        }
        failure {
            mail to: 'mandadij@uwindsor.ca',
                 subject: "Build failed in Jenkins: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                 body: "Something is wrong in ${env.JOB_NAME} #${env.BUILD_NUMBER}\nPlease check Jenkins for details."
        }
    }
}
